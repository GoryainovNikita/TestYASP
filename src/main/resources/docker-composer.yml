version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: test_task
    environment:
      POSTGRES_DB: "books"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"